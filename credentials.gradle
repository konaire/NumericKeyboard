// transferring properties from the `local.properties` to the gradle's global scope
Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())

ext["signing.keyId"] = properties.getProperty("ARTIFACT_SIGNING_KEY_ID")
ext["signing.password"] = properties.getProperty("ARTIFACT_SIGNING_PASSWORD")
ext["signing.secretKeyRingFile"] = properties.getProperty("ARTIFACT_SIGNING_KEYRING_PATH")
